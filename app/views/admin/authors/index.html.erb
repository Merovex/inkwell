<% content_for :title, "Authors — Inkwell" %>

<%= render "admin/shared/canvas_head", crumbs: [["Authors", nil]], menu: false do %>
  <%= link_to new_admin_author_path, class: "button button--primary" do %>
    <%= inline_svg_tag "lucide/plus.svg", class: "lucide", size: "16px" %> New author
  <% end %>
<% end %>

<header class="perma-header">
  <h1 class="perma-header__title">Authors</h1>
</header>

<% if @authors.any? %>
  <div class="list">
    <% @authors.each do |author| %>
      <%= render "shared/list_item",
            href: edit_admin_author_path(author),
            title: author.name,
            avatar: false,
            badge: ("Default" if author.default?),
            badge_variant: "accent",
            discard_path: admin_author_path(author),
            discard_title: "Remove",
            discard_aria: "Remove #{author.name}",
            discard_confirm: "Remove #{author.name}?" %>
    <% end %>
  </div>
<% else %>
  <div class="empty">
    <span class="empty__icon"><%= inline_svg_tag "lucide/smile-plus.svg", class: "lucide", size: "40px" %></span>
    <p class="empty__title">No authors yet</p>
    <p class="empty__text">Add a pen name to use as a byline on posts, books, and series.</p>
    <div class="empty__actions"><%= link_to "New author", new_admin_author_path, class: "button button--primary" %></div>
  </div>
<% end %>

<% content_for :title, "New promotion" %>

<%= render "admin/shared/canvas_head", crumbs: [["Promotions", admin_promotions_path], ["New", nil]] %>

<header class="perma-header">
  <h1 class="perma-header__title">New promotion</h1>
</header>

<div class="u-center u-center-narrow">
  <%= render "form", promotion: @promotion, url: admin_promotions_path %>
</div>

<% content_for :title, "New campaign — Inkwell" %>

<%= render "admin/shared/canvas_head", crumbs: [["Campaigns", admin_drips_path], ["New campaign", nil]] %>

<%= render "form", drip: @drip, url: admin_drips_path %>

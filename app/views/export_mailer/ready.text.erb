Your export is ready.

The copy of <%= @site_name %> you asked for is built: every post, page, book,
newsletter, and drip, plus your subscriber list.

Sign in to download it:

  <%= @exports_url %>

It includes your readers' email addresses, so it's only available to you, signed
in, and it's deleted on <%= @export.expires_at.to_date.to_fs(:long) %>. You can request a fresh one
any time.

— Kindred Quill

<% content_for :title, "New category — Alcovo" %>

<%= render "shared/canvas_head", crumbs: [["Forum", messages_path], ["Categories", categories_path], ["New category", nil]] %>

<header class="perma-header">
  <h1 class="perma-header__title">New category</h1>
</header>

<div class="u-center u-center-narrow">
  <%= render "form", category: @category %>
</div>

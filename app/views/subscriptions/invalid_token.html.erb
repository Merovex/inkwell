<% content_for :title, "Link expired — #{site_settings.site_name}" %>

<section class="press-shell">
  <header class="press-header u-flow u-flow-half">
    <h1 class="press-display-lg">This link is invalid or has expired</h1>
    <p class="press-muted">The link may have already been used. You can <%= link_to "start over", newsletter_path, class: "press-link" %>.</p>
  </header>
</section>

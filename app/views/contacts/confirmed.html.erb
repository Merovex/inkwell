<% content_for :title, "Message confirmed — #{site_settings.site_name}" %>

<section class="press-article">
  <header class="press-article__head">
    <h1 class="press-article__title">Message confirmed</h1>
    <p class="press-article__byline">
      Thanks — your message has reached <%= site_settings.site_name %>. We'll be in touch.
    </p>
  </header>
  <div class="press-article__body">
    <p><%= link_to "Back to home", root_path, class: "press-link" %></p>
  </div>
</section>

<% content_for :title, "Edit bulletin" %>
<% content_for :canvas_class, "canvas--fill" %>

<%= render "admin/shared/canvas_head", crumbs: [["Bulletins", support_bulletins_path], [@bulletin.title, nil]], menu: false do %>
  <%= render "composer_actions", bulletin: @bulletin %>
<% end %>

<%= render "form", bulletin: @bulletin, url: support_bulletin_path(@record) %>
